# vertx-event-stream

Event stream architecture, with websocket and web subscribers

### Web Hook (SSE)
`curl localhost:8080/events`

### WebSocket Hook
`node client.js`

### Docker
`docker compose up`
